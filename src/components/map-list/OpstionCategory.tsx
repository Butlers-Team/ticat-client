import styled from 'styled-components';

const OpstionCategory = () => {
  return <OpstionList></OpstionList>;
};

export default OpstionCategory;

const OpstionList = styled.aside`
  height: 60px;
  border-bottom: 1px solid rgba(173, 173, 173, 0.2);
`;
